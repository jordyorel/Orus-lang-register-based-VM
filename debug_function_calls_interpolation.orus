fn main() {
    let arr = [1, 2, 3]
    let x = 42
    
    // Test different function calls in interpolation
    print("Stored results:")
    let length = len(arr)
    print("Length: {}", length)
    
    print("Direct function calls:")
    print("Length: {}", len(arr))
}