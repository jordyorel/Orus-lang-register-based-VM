// Simple recursive function to test self references
fn countdown(n: i32) {
    if n <= (0 as i32) {
        return
    }
    countdown(n - (1 as i32))
}

fn main() {
    countdown(5 as i32)
    print("done")
}
