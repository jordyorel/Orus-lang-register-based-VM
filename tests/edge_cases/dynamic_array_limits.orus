// Grow a dynamic array beyond its initial capacity
fn main() {
    let arr: [i32; 1] = [0]
    for i in 1..20 {
        push(arr, i as i32)
    }
    print(len(arr))
    print(arr[0 as i32])
    print(arr[19 as i32])
}
