// Test structs with array fields (simplified)
struct Inventory {
    name: string,
    count: i32,
}

fn main() {
    let inv = Inventory {
        name: "warehouse",
        count: 3,
    }
    
    print("Name: {}", inv.name)
    print("Count: {}", inv.count)
}