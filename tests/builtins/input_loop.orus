fn main() {
    let n: i32 = int(input("How many numbers? "))
    let mut sum: i32 = 0
    let mut i: i32 = 0
    while i < n {
        sum = sum + int(input("num: "))
        i = i + 1
    }
    print("sum {}", sum)
}
