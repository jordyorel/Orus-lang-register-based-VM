fn main() {
    let values: [f64] = [3.5, 2.0, 4.1]
    print(max(values))

    let arr: [i32; 1] = [0]
    push(arr, 10 as i32)
    push(arr, -3 as i32)
    push(arr, 7 as i32)
    print(max(arr))
}
