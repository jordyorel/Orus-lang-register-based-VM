fn main() {
    let values: [f64] = [1.5, 2.5, 3.0]
    print(sum(values))

    let arr: [i32; 1] = [0]
    push(arr, 10 as i32)
    push(arr, 20 as i32)
    push(arr, -5 as i32)
    print(sum(arr))
}
