fn add_one(x: i64) -> i64 {
    return x + (1 as i64)
}

fn add_two(x: i64) -> i64 {
    return add_one(add_one(x))
}

fn add_three(x: i64) -> i64 {
    return add_two(add_one(x))
}

fn main() {
    print(add_three(5 as i64))
}
