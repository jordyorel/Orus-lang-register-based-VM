// Minimal test for continue statement
fn test_continue() -> i32 {
    let sum = 0
    for i in 0..5 {
        if i == 2 {
            continue
        }
        sum = sum + i
    }
    return sum  // Should be 0+1+3+4 = 8
}

print("Continue test result:")
print(test_continue())