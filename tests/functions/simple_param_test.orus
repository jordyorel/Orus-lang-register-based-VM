fn test_params(a: i32, b: i32) -> i32 {
    // Just print the parameters to see what's on the stack
    print("a = {}", a)
    print("b = {}", b)
    return a + b
}

fn main() {
    let result: i32 = test_params(42, 99)
    print("Result: {}", result)
}